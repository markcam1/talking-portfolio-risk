# Database
DATABASE_URL=file:./data/talking-portfolio.sqlite

# Orchestrator
PORT=5179
WEB_PORT=5180

# Optimizer Web (leave blank in dev — Vite proxy handles it; set for production builds)
VITE_OPTIMIZER_API_URL=http://127.0.0.1:8077

# Upstream services
OPTIMIZER_BASE_URL=http://127.0.0.1:8077
CALL_AGENT_BASE_URL=http://127.0.0.1:3334

# Storage
DB_PATH=./data/talking-portfolio.sqlite
DATA_DIR=./data
LOG_DIR=./logs

# Compliance / dialing policy
OWNED_NUMBERS_ONLY=true
DEFAULT_POLICY_ID=self
SELF_CALL_IGNORE_WINDOW=true
CALLING_WINDOW_START=08:00
CALLING_WINDOW_END=21:00
FREQUENCY_CAP_PER_DAY=1
MOCK_MODE=true
# When MOCK_MODE=true, override the dialed number (leave blank to use the job's phoneE164)
MOCK_CALL_TARGET=

# Email — SMTP (use Mailpit in dev: web UI :8025, SMTP :1025)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="Your Name <noreply@example.com>"
COMPLIANCE_BCC=

# Default CallerProfile seed (editable in the web app)
DEFAULT_ENTITY_NAME="Your Name"
DEFAULT_CALLBACK_NUMBER=+15550001234

# Feature flags — all off for MVP
APPROVAL_REQUIRED=false
EVENT_TRIGGERS_ENABLED=false
INBOUND_ENABLED=false
